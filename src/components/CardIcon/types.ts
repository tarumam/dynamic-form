import { BaseComponentInterface } from "../CommonTypes/baseComponentInterface";

export interface cardIconProps extends BaseComponentInterface {
  icon: string;
  text: string;
  alt: string;
}

export interface cardIconContainerType extends BaseComponentInterface {
}
